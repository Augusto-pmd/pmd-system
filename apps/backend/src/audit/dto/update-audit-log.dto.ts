// Audit logs are immutable, so update DTO is not needed
// This file is kept for consistency but should not be used
export class UpdateAuditLogDto {
  // No updates allowed - audit logs are immutable
}

