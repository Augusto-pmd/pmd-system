# PMD Management System - Development Environment Configuration
# This file is for local development only
# Production uses DATABASE_URL from Render

# ============================================
# Database Configuration (Local Development)
# ============================================
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=pmd_management

# DATABASE_URL is NOT set in development - uses individual variables above
# In production (Render), set DATABASE_URL instead

# ============================================
# JWT Authentication
# ============================================
JWT_SECRET=supersecret123
JWT_EXPIRATION=1d

# ============================================
# Application Configuration
# ============================================
PORT=3000
NODE_ENV=development
